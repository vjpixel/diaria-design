**Logo** — the Diar.ia wordmark rendered as live text (Geist 600), with the dots and `.br` in brand teal.

```jsx
<Logo size={20} />
<Logo size={28} onDark />
<Logo tld={false} />   {/* diar.ia, no .br */}
```

For print, favicons, or anywhere the font may be missing, use the vector/PNG files in `assets/logo/` instead of this live-text version.
