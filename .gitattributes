# Normalize line endings: store as LF in the repo, let Git handle the working copy
* text=auto eol=lf

# Treat binary assets as-is (no EOL conversion)
*.png binary
*.jpg binary
*.pdf binary
*.svg text eol=lf
