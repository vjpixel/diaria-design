**Rule** — the horizontal newspaper rule that brackets mastheads and separates sections.

```jsx
<Rule thick />          {/* 2px solid ink — masthead brackets */}
<Rule />                {/* 1px hairline — section dividers */}
<Rule onDark style={{ marginTop: 64 }} />
```

Prefer rules over shadows for separation — the system is flat and print-like.
